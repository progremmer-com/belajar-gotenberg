# Gotenberg with Docker Compose

Pastikan sudah install [docker](https://docs.docker.com/engine/install/) dan [docker-compose](https://docs.docker.com/compose/install/) di VM/PC

## Installation

```bash
$ docker-compose up -d --build
```